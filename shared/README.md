# Shared

O frontend agora é implementado em Python com NiceGUI, então não há mais pacote npm compartilhado.
Contratos entre interface e API devem ser mantidos nos schemas Pydantic do backend (`backend/app/schemas`).
